import React from 'react';
import ReactDOM from 'react-dom';
import LoginSighup from './LoginSighup';

ReactDOM.render(
  <React.StrictMode>
    <LoginSighup />
  </React.StrictMode>,
  document.getElementById('root')
);